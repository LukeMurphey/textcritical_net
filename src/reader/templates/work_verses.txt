{% load reader_extras %}{% for verse in verses.all %}{% if verse.indicator|length > 0 %}{{verse.indicator}} {% endif %}{% if verse.original_content|length == 0 %}{{verse.content|safe}}{% else %}{{verse.original_content|perseus_xml_to_text:chapter.work.language|safe}}{% endif %}{% endfor %}